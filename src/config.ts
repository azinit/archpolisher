export default {
    // [GH_] files: [num=1, rad=0.05]
    // [FSD] files: [num=1, rad=0.06]
    // [FSD] modules: [num=1, rad=0.06+]
    "fixtures": "GH_FDD__SPEC",
    "strategy": "modules",
    "clustering": {
        "neighNum": 1,
        "neighRadius": 0.05
    }
}